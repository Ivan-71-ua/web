# Normalize line endings — prefer LF in repository and working tree where appropriate
# This avoids warnings about LF/CRLF and keeps consistent EOL across platforms.

# Treat all files as text by default when possible
* text=auto

# Force LF for common source files so they stay consistent in the repo and on CI
*.js     text eol=lf
*.jsx    text eol=lf
*.ts     text eol=lf
*.tsx    text eol=lf
*.json   text eol=lf
*.css    text eol=lf
*.scss   text eol=lf
*.html   text eol=lf
*.md     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.sh     text eol=lf

# Keep Windows-specific scripts with CRLF
*.cmd    text eol=crlf
